#include "time.h"

time_t startCount=time(NULL);

time_t endCount=time(NULL+3);

int countDown(int sec){
  while((startCount-endCount)<sec){
    if (harware obstructed(1)){
      countDown(sec)
    }
    if (harware stop(1)){
      countdown(sec)
    }
//possibility to add bestillinger
  }
  return 1;
}
